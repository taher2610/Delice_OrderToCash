<form method="post" action="{{ path('app_archive_delete', {'id': archive.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ archive.id) }}">
    <button class="btn">Delete</button>
</form>
